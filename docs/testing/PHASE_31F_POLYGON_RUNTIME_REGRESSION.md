# Phase 31F Polygon Runtime Regression

Regression shape: Polygon mode, two logical LAS inputs, EPSG:6635 assumed coordinate-space alignment, CHM and Rumple, spatial preflight ready.

Before coordinator submission, the authoritative verifier executes with the managed backend Python and requires `pyforestscan.handlers` plus all normal processing modules. The preflight and execution runtime identities must match. A failed contract blocks submission with an engine setup/repair error and creates no scientific batch attempt.

QGIS-free regression coverage proves that `READY` cannot coexist with missing `pyforestscan.handlers`, protocol/identity faults are classified before launch, and the quick manifest cache does not spawn a process. Actual Olaa source files were not available in the repository test environment, so HAG/CHM/Rumple live completion remains to be recorded on the Windows test machine.
