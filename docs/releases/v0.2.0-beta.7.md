# PyForestScan QGIS v0.2.0-beta.7

Fixes the Voxel Statistic planning gate introduced in beta.6. Dataset Explorer now reports voxel-stat feasibility so an `Intensity` raster can proceed into the validated processing pipeline.

Install the distinct `pyforestscan_qgis-v0.2.0-beta.7.zip` artifact; prior versioned ZIPs remain available.
