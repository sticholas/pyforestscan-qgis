# Plugin Package

This directory is reserved for the future QGIS plugin package.

It will eventually contain plugin entry points, Processing provider
registration, algorithms, resources, styles, icons, and plugin-owned support
code. No plugin functionality is implemented in Phase 0.

