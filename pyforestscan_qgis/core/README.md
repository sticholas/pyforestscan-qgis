# Core

This directory will contain plugin-owned core services such as dependency
validation, parameter validation, output naming, metadata capture, and adapter
boundaries around PyForestScan public APIs.

Core code should be testable outside QGIS whenever practical.

